# Added per http://www.phoenixframework.org/docs/heroku
# This has to do with stale dependencies which are not getting recompiled properly.
# It's possible to force Heroku to recompile all dependencies on each deploy, which should fix this problem.
always_rebuild=true